/**
 * Footer component.
 * Creates the site footer element.
 * Note: No footer section was detected in the original jamesbuckhouse.com site.
 */

// TODO: Implement footer content if/when design is provided
export function createFooter() {
  const footer = document.createElement('footer');
  footer.className = 'site-footer';
  return footer;
}

export default createFooter;
