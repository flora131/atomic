/**
 * Compatibility barrel for the sub-agent stream adapter.
 *
 * The implementation now lives under `events/adapters/subagent-adapter/`,
 * while the historical `subagent-adapter.ts` path remains stable.
 */

export * from "@/services/events/adapters/subagent-adapter/index.ts";
