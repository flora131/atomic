/**
 * Compatibility barrel for event bus types and schemas.
 *
 * The implementation now lives under `events/bus-events/`, while the
 * historical `events/bus-events.ts` path remains stable.
 */

export * from "@/services/events/bus-events/index.ts";
