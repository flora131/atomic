/**
 * Compatibility barrel for the graph builder DSL.
 *
 * The implementation now lives under `graph/authoring/builder.ts`, while the
 * historical `graph/builder.ts` path remains stable.
 */

export * from "@/services/workflows/graph/authoring/builder.ts";
